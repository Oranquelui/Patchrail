# Patchrail Nightly Dev - 2026-06-23

## 1. Today's conclusion

Selected one small Runner Contract v1 slice: the built-in local harness now records runtime compatibility evidence for the reserved runner environment and workspace paths. The trace shows which reserved variables were observed, how task/plan/output/artifact/trace paths resolve relative to `PATCHRAIL_WORKSPACE`, and which runner-writable paths were ready during execution.

This strengthens the local runner workspace path contract without changing canonical `Task / Plan / Run / Review / Approval / Ledger` semantics, making breaking runner-interface changes, weakening approval boundaries, or integrating subordinate repositories.

## 2. Work completed

- Inspected repository state, current branch, recent commit history, MVP and architecture docs, Runner Contract docs, local testing docs, tests, CLI flow, runner code, artifact code, and latest nightly report.
- Created branch `codex/nightly/2026-06-23-patchrail` while preserving carried-forward uncommitted nightly work already in the worktree.
- Confirmed the new focused runtime trace test failed before implementation because `runner_trace.contract_runtime` was missing.
- Added `contract_runtime` to the local harness trace with:
  - `reserved_environment_present`
  - `workspace_relative_paths`
  - `runner_writable_paths_ready`
- Mirrored the local harness structured trace to `PATCHRAIL_TRACE_FILE` inside the per-run workspace.
- Added smoke coverage proving the canonical artifact trace includes the runtime contract evidence.
- Fixed an existing broader Runner Contract metadata mismatch by adding `runner_contract_schema_version`, `artifact_dir`, and `trace_file` to the invocation artifact created by `PatchrailApp.run_task()`.
- Updated Runner Contract, architecture, local testing, and backlog docs to describe the runtime compatibility trace.

## 3. Files changed

Tonight's Runner Contract runtime-evidence slice changed:

- `docs/architecture.md`
- `docs/backlog.md`
- `docs/local-testing.md`
- `docs/contracts/runner-contract-v1.md`
- `patchrail/core/service.py`
- `patchrail/runners/local_harness.py`
- `tests/test_layer_harness_contract.py`
- `tests/test_local_smoke_script.py`
- `reports/nightly/2026-06-23.md`

The worktree also still contains carried-forward uncommitted changes from prior nightly slices, including Brief Schema v1, Evidence Bundle v1, Runner Contract v1 CLI inspection, local smoke updates, and related tests/docs. Those were preserved and not reverted.

## 4. Tests run

- Red test before implementation:
  - `/opt/homebrew/bin/python3.13 -m pytest tests/test_layer_harness_contract.py::test_local_harness_trace_declares_harness_contract -q`
  - Failed as expected with `KeyError: 'contract_runtime'`.
- Focused runtime trace test:
  - `/opt/homebrew/bin/python3.13 -m pytest tests/test_layer_harness_contract.py::test_local_harness_trace_declares_harness_contract -q`
  - `1 passed in 0.09s`
- Focused contract and smoke tests:
  - `/opt/homebrew/bin/python3.13 -m pytest tests/test_layer_harness_contract.py tests/test_local_smoke_script.py -q`
  - `9 passed in 1.41s`
- Full test suite, first pass:
  - `/opt/homebrew/bin/python3.13 -m pytest -q`
  - Failed because `invocation.json` did not include `runner_contract_schema_version`.
- Focused regression after invocation metadata fix:
  - `/opt/homebrew/bin/python3.13 -m pytest tests/test_cli_flow.py::test_happy_path_persists_state_artifacts_and_ledgers -q`
  - `1 passed in 0.15s`
- Full test suite, final pass:
  - `/opt/homebrew/bin/python3.13 -m pytest -q`
  - `76 passed, 2 skipped in 5.63s`
- CLI help:
  - `/opt/homebrew/bin/python3.13 -m patchrail.cli --help`
  - Passed and listed `contracts` among top-level commands.
- JSON contract inspection:
  - `/opt/homebrew/bin/python3.13 -m patchrail.cli --json contracts runner`
  - Passed and returned `schema_version=patchrail.runner_contract.v1`.
- Human-readable contract inspection:
  - `/opt/homebrew/bin/python3.13 -m patchrail.cli contracts runner`
  - Passed and printed workspace files, reserved environment, runner-writable paths, and forbidden ownership boundaries.
- Static bytecode check:
  - `/opt/homebrew/bin/python3.13 -m compileall -q patchrail`
  - Passed.
- Local smoke:
  - `PYTHON_BIN=/opt/homebrew/bin/python3.13 PATCHRAIL_HOME=<tmp>/.patchrail PATCHRAIL_CONFIG_PRESET=local PATCHRAIL_WORKFLOW_BACKEND=local sh scripts/local_smoke_test.sh`
  - Passed with final state `approved` for `task_6932b098deda` / `run_0584812fafdd`.

No configured lint, type, or static-analysis tool was found in `pyproject.toml` beyond pytest.

## 5. Evidence Bundle

- Runner Contract evidence: `patchrail --json contracts runner` exposes `schema_version=patchrail.runner_contract.v1`, `owns_canonical_state=false`, workspace files `task.json`, `plan.json`, and `output.json`, reserved shell environment variables, runner-writable paths, and forbidden ownership of task lifecycle, canonical plan, review verdict, approval decision, and approval ledger.
- Runtime trace evidence: the local harness now emits `runner_trace.contract_runtime` with all eight reserved environment variables observed, workspace-relative handoff paths for `task.json`, `plan.json`, `output.json`, `artifacts`, and `trace.json`, and ready runner-writable paths `output.json`, `artifacts/`, and `trace.json`.
- Workspace trace evidence: the local harness writes the same structured trace to `.patchrail/workspaces/<run_id>/trace.json` via `PATCHRAIL_TRACE_FILE`.
- Canonical artifact evidence: smoke coverage verifies the runtime contract trace is persisted under the canonical Evidence Bundle v1 `runner_trace` artifact without giving the runner ownership of canonical run, review, approval, or ledger state.
- Invocation evidence: `invocation.json` now records `runner_contract_schema_version`, `artifact_dir`, and `trace_file` alongside the existing runner command, mode, workspace, task, plan, output, and exit-code metadata.
- Test evidence: focused contract/smoke tests passed, full pytest passed, CLI help passed, JSON and human contract inspection passed, compileall passed, and local smoke completed through approval.
- Safety evidence: no secrets, credentials, migrations, deployments, browser MCP registration, browser debugging, or direct subordinate repository integrations were used.

## 6. Next highest-priority task

Continue Runner Contract v1 by adding narrow runner-local artifact ingestion from `.patchrail/workspaces/<run_id>/artifacts/` into canonical evidence metadata, while keeping Patchrail as the owner of artifact bundles, reviews, approvals, ledgers, and task lifecycle state.

## 7. Approval required: yes/no

No.
