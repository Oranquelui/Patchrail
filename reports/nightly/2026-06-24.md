# Patchrail Nightly Dev - 2026-06-24

## 1. Today's conclusion

Selected one small Runner Contract v1 slice: Patchrail now canonicalizes runner-local artifact files written under the reserved workspace `artifacts/` directory. The runner still only writes exchange files; Patchrail copies regular non-symlink files into `.patchrail/artifacts/<run_id>/runner-artifacts/` and records them in the Evidence Bundle v1 manifest with logical kind `runner_artifact`.

This extends Runner Contract v1 evidence without changing canonical `Task / Plan / Run / Review / Approval / Ledger` semantics, making breaking runner-interface changes, weakening approval boundaries, or integrating subordinate repositories.

## 2. Work completed

- Inspected repository state, the current branch, MVP and architecture docs, Runner Contract docs, Evidence Bundle docs, local testing docs, runner code, artifact service code, contract tests, smoke tests, and the latest nightly report.
- Created branch `codex/nightly/2026-06-24-patchrail` while preserving carried-forward uncommitted nightly work already in the worktree.
- Confirmed the new focused smoke test failed before implementation because the canonical bundle did not contain `runner_artifact:local-harness-report.json`.
- Added Patchrail-owned runner artifact collection in `ArtifactService.create_bundle()`.
- Passed the reserved workspace artifact directory from `PatchrailApp.run_task()` into artifact bundle creation.
- Updated the local harness to write `local-harness-report.json` into `PATCHRAIL_ARTIFACT_DIR`.
- Added a symlink guard so runner artifact collection only copies regular non-symlink files.
- Added smoke coverage proving the runner-local artifact is copied into canonical artifact storage and exposed through the Evidence Bundle manifest.
- Updated Runner Contract v1, Evidence Bundle v1, architecture, local testing, README, and backlog docs to describe the new collection path.

## 3. Files changed

Tonight's Runner Contract artifact-ingestion slice changed:

- `README.md`
- `docs/architecture.md`
- `docs/backlog.md`
- `docs/local-testing.md`
- `docs/contracts/evidence-bundle-v1.md`
- `docs/contracts/runner-contract-v1.md`
- `patchrail/artifacts/service.py`
- `patchrail/core/layers.py`
- `patchrail/core/service.py`
- `patchrail/runners/local_harness.py`
- `tests/test_layer_harness_contract.py`
- `tests/test_local_smoke_script.py`
- `reports/nightly/2026-06-24.md`

The worktree also still contains carried-forward uncommitted changes from prior nightly slices, including Brief Schema v1, Evidence Bundle v1, Runner Contract v1 CLI inspection, local smoke updates, and related tests/docs. Those were preserved and not reverted.

## 4. Tests run

- Red test before implementation:
  - `/opt/homebrew/bin/python3.13 -m pytest tests/test_local_smoke_script.py::test_local_smoke_script_persists_v1_briefs_and_plan_references -q`
  - Failed as expected because `runner_artifact:local-harness-report.json` was missing from the bundle manifest.
- Focused regression after implementation:
  - `/opt/homebrew/bin/python3.13 -m pytest tests/test_local_smoke_script.py::test_local_smoke_script_persists_v1_briefs_and_plan_references tests/test_layer_harness_contract.py::test_evidence_bundle_contract_declares_v1_manifest_boundary -q`
  - `2 passed in 1.42s`
- Focused contract and smoke tests:
  - `/opt/homebrew/bin/python3.13 -m pytest tests/test_layer_harness_contract.py tests/test_local_smoke_script.py -q`
  - `9 passed in 1.44s`
- Full test suite:
  - `/opt/homebrew/bin/python3.13 -m pytest -q`
  - `76 passed, 2 skipped in 6.01s`
- CLI help:
  - `/opt/homebrew/bin/python3.13 -m patchrail.cli --help`
  - Passed and listed `contracts` among top-level commands.
- JSON contract inspection:
  - `/opt/homebrew/bin/python3.13 -m patchrail.cli --json contracts runner`
  - Passed and returned `schema_version=patchrail.runner_contract.v1`.
- Human-readable contract inspection:
  - `/opt/homebrew/bin/python3.13 -m patchrail.cli contracts runner`
  - Passed and printed workspace files, reserved environment, runner-writable paths, and forbidden ownership boundaries.
- Static bytecode check:
  - `/opt/homebrew/bin/python3.13 -m compileall -q patchrail`
  - Passed.
- Local smoke:
  - `PYTHON_BIN=/opt/homebrew/bin/python3.13 PATCHRAIL_HOME=<tmp>/.patchrail PATCHRAIL_CONFIG_PRESET=local PATCHRAIL_WORKFLOW_BACKEND=local sh scripts/local_smoke_test.sh`
  - Passed with final state `approved` for `task_aa3bbf21b86d` / `run_6c6eb89a20b1`.
- Read-side runner artifact query:
  - `PATCHRAIL_HOME=<tmp>/.patchrail /opt/homebrew/bin/python3.13 -m patchrail.cli --json list artifact-bundles --logical-kind runner_artifact`
  - Passed and returned `runner_artifact:local-harness-report.json` with `logical_kind=runner_artifact`, `media_type=application/json`, digest, size, and canonical path under `runner-artifacts/`.

No configured lint, type, or static-analysis tool was found in `pyproject.toml` beyond pytest.

## 5. Evidence Bundle

- Runner Contract evidence: `patchrail --json contracts runner` still exposes `schema_version=patchrail.runner_contract.v1`, workspace files `task.json`, `plan.json`, and `output.json`, the reserved shell environment, runner-writable paths `output.json`, `artifacts/`, and `trace.json`, and forbidden ownership of task lifecycle, canonical plan, review verdict, approval decision, and approval ledger.
- Evidence Bundle evidence: `patchrail.evidence_bundle.v1` now declares optional logical kinds `runner_trace` and `runner_artifact`.
- Runner artifact evidence: local harness writes `local-harness-report.json` to `PATCHRAIL_ARTIFACT_DIR`; Patchrail copies it into `.patchrail/artifacts/<run_id>/runner-artifacts/local-harness-report.json` and records manifest key `runner_artifact:local-harness-report.json`.
- Safety evidence: runner artifact collection skips symlinks and only copies regular files from the runner-local artifact directory.
- Read-side evidence: `patchrail list artifact-bundles --logical-kind runner_artifact` returns the canonical runner artifact manifest entry with digest and byte size.
- Test evidence: focused contract/smoke tests passed, full pytest passed, CLI help passed, JSON and human contract inspection passed, compileall passed, and local smoke completed through approval.
- Boundary evidence: no secrets, credentials, migrations, deployments, browser MCP registration, browser debugging, or direct subordinate repository integrations were used.

## 6. Next highest-priority task

Continue Runner Contract v1 by adding narrow validation rules for runner-local artifact size, naming, and media-type limits before teaching the contract through a reusable Patchrail Codex skill.

## 7. Approval required: yes/no

No.
