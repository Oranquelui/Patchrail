# Patchrail Nightly Dev - 2026-06-21

## 1. Today's conclusion

Selected one small vertical slice under Runner Contract v1: Patchrail now exposes the existing runner workspace and reserved environment boundary through a read-only CLI inspection command, `patchrail contracts runner`.

This improves operator reproducibility without changing canonical `Task / Plan / Run / Review / Approval / Ledger` semantics, making breaking runner-interface changes, weakening approval boundaries, or integrating subordinate repositories.

## 2. Work completed

- Inspected repository state, current branch, recent commit history, MVP and architecture docs, latest nightly report, Runner Contract docs, CLI entrypoints, service layer, renderer, tests, local testing guide, and backlog.
- Created branch `codex/nightly/2026-06-21-patchrail` while preserving carried-forward uncommitted nightly work already in the worktree.
- Confirmed the new contract inspection test failed before implementation because `contracts` was not a recognized CLI command.
- Added `patchrail contracts runner` and `patchrail --json contracts runner`.
- Added `PatchrailApp.get_runner_contract()` as a read-only service method returning `RUNNER_CONTRACT.to_dict()`.
- Added human-readable rendering for the Runner Contract v1 schema, workspace files, reserved environment variables, runner-writable paths, and forbidden ownership boundaries.
- Updated Runner Contract, README, architecture, local testing, and backlog docs to include the new operator inspection command.

## 3. Files changed

Tonight's Runner Contract v1 inspection slice changed:

- `README.md`
- `docs/architecture.md`
- `docs/backlog.md`
- `docs/local-testing.md`
- `docs/contracts/runner-contract-v1.md`
- `patchrail/cli/main.py`
- `patchrail/cli/render.py`
- `patchrail/core/service.py`
- `tests/test_layer_harness_contract.py`
- `reports/nightly/2026-06-21.md`

The worktree also still contains carried-forward uncommitted changes from prior nightly slices, including Brief Schema v1, Evidence Bundle v1, Runner Contract v1, local smoke, and related tests/docs. Those were preserved and not reverted.

## 4. Tests run

- Initial environment check:
  - `python3 -m pytest tests/test_layer_harness_contract.py::test_cli_exposes_runner_contract_for_operator_inspection -q`
  - Did not run project tests because default `python3` points at Python 3.14 without pytest.
- Red test before implementation:
  - `/opt/homebrew/bin/python3.13 -m pytest tests/test_layer_harness_contract.py::test_cli_exposes_runner_contract_for_operator_inspection -q`
  - Failed as expected because `contracts` was not a recognized CLI command.
- Focused contract tests:
  - `/opt/homebrew/bin/python3.13 -m pytest tests/test_layer_harness_contract.py -q`
  - `8 passed in 0.11s`
- Full test suite:
  - `/opt/homebrew/bin/python3.13 -m pytest -q`
  - `76 passed, 2 skipped in 6.16s`
- CLI help:
  - `/opt/homebrew/bin/python3.13 -m patchrail.cli --help`
  - Passed and listed `contracts` among top-level commands.
- JSON contract inspection:
  - `/opt/homebrew/bin/python3.13 -m patchrail.cli --json contracts runner`
  - Passed and returned `schema_version=patchrail.runner_contract.v1`.
- Human-readable contract inspection:
  - `/opt/homebrew/bin/python3.13 -m patchrail.cli contracts runner`
  - Passed and printed workspace files, reserved environment, runner-writable paths, and forbidden ownership boundaries.
- Static bytecode check:
  - `/opt/homebrew/bin/python3.13 -m compileall -q patchrail`
  - Passed.
- Local smoke:
  - `PYTHON_BIN=/opt/homebrew/bin/python3.13 PATCHRAIL_HOME=<tmp>/.patchrail PATCHRAIL_CONFIG_PRESET=local PATCHRAIL_WORKFLOW_BACKEND=local sh scripts/local_smoke_test.sh`
  - Passed with final state `approved` for `task_d1f212d76809` / `run_278b63408dae`.

No configured lint, type, or static-analysis tool was found in `pyproject.toml` beyond pytest.

## 5. Evidence Bundle

- Contract inspection evidence: `patchrail --json contracts runner` exposes `schema_version=patchrail.runner_contract.v1`, `owns_canonical_state=false`, workspace files `task.json`, `plan.json`, and `output.json`, reserved shell environment variables, runner-writable paths, and forbidden ownership of task lifecycle, canonical plan, review verdict, approval decision, and approval ledger.
- Human-readable evidence: `patchrail contracts runner` prints the same boundary for CLI operators without creating tasks, runs, artifacts, reviews, approvals, or ledger entries.
- Test evidence: focused contract tests passed, full pytest passed, CLI help included `contracts`, compileall passed, and local smoke completed through approval.
- Safety evidence: no secrets, credentials, migrations, deployments, browser MCP registration, browser debugging, or direct subordinate repository integrations were used.

## 6. Next highest-priority task

Continue Runner Contract v1 by adding compatibility/readiness checks for shell runner contract support and richer runner-local artifact ingestion, while keeping Patchrail as the canonical owner of evidence bundles, reviews, approvals, and ledgers.

## 7. Approval required: yes/no

No.
