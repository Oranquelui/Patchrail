# Patchrail Nightly Dev - 2026-06-20

## 1. Today's conclusion

Selected one small vertical slice under Runner Contract v1: Patchrail now declares `schema_version=patchrail.runner_contract.v1` for the runner workspace and reserved environment boundary, shell runners receive additive artifact and trace path variables, and the local harness trace exposes the contract without giving runners ownership of canonical state.

This advances the highest current priority without changing canonical `Task / Plan / Run / Review / Approval / Ledger` semantics, making breaking runner-interface changes, weakening approval boundaries, or integrating subordinate repositories.

## 2. Work completed

- Inspected repository status, branch, README, MVP and architecture docs, backlog, runner code, tests, docs/contracts, and the latest nightly report.
- Created branch `codex/nightly/2026-06-20-patchrail` while preserving existing uncommitted nightly work already in the worktree.
- Confirmed the Runner Contract v1 test failed before implementation because `patchrail.core.layers` did not expose `RUNNER_CONTRACT`.
- Added `RunnerContract` and `RUNNER_CONTRACT` in `patchrail.core.layers`.
- Added additive shell runner environment variables: `PATCHRAIL_WORKSPACE`, `PATCHRAIL_ARTIFACT_DIR`, and `PATCHRAIL_TRACE_FILE`.
- Updated the local harness trace to include `runner_contract` plus resolved workspace exchange paths.
- Added `docs/contracts/runner-contract-v1.md`.
- Updated README, architecture, backlog, and local testing docs to describe the Runner Contract v1 workspace and reserved environment boundary.
- Extended smoke-path coverage to verify the persisted runner trace includes `patchrail.runner_contract.v1`.

## 3. Files changed

Tonight's Runner Contract v1 slice changed:

- `README.md`
- `docs/architecture.md`
- `docs/backlog.md`
- `docs/local-testing.md`
- `docs/contracts/runner-contract-v1.md`
- `patchrail/core/layers.py`
- `patchrail/runners/local_harness.py`
- `patchrail/runners/stub.py`
- `tests/test_layer_harness_contract.py`
- `tests/test_local_smoke_script.py`
- `reports/nightly/2026-06-20.md`

The worktree also still contains carried-forward uncommitted changes from prior nightly slices, including Brief Schema v1 and Evidence Bundle v1 files/tests. Those were preserved and not reverted.

## 4. Tests run

- Red test before implementation:
  - `/opt/homebrew/bin/python3.13 -m pytest tests/test_layer_harness_contract.py::test_runner_contract_declares_v1_workspace_and_env_boundary -q`
  - Failed as expected because `patchrail.core.layers` did not expose `RUNNER_CONTRACT`.
- Focused tests after implementation:
  - `/opt/homebrew/bin/python3.13 -m pytest tests/test_layer_harness_contract.py::test_runner_contract_declares_v1_workspace_and_env_boundary tests/test_layer_harness_contract.py::test_local_harness_trace_declares_harness_contract -q`
  - `2 passed in 0.11s`
- Contract and smoke-path tests:
  - `/opt/homebrew/bin/python3.13 -m pytest tests/test_layer_harness_contract.py tests/test_local_smoke_script.py -q`
  - `8 passed in 1.45s`
- Full test suite:
  - `/opt/homebrew/bin/python3.13 -m pytest -q`
  - `75 passed, 2 skipped in 12.86s`
- CLI help:
  - `/opt/homebrew/bin/python3.13 -m patchrail.cli --help`
  - Passed and printed the top-level command list.
- Static bytecode check:
  - `/opt/homebrew/bin/python3.13 -m compileall -q patchrail`
  - Passed.
- Local smoke:
  - `PYTHON_BIN=/opt/homebrew/bin/python3.13 PATCHRAIL_HOME=<tmp>/.patchrail PATCHRAIL_CONFIG_PRESET=local PATCHRAIL_WORKFLOW_BACKEND=local sh scripts/local_smoke_test.sh`
  - Passed with final state `approved` for `task_449df6006b09` / `run_8ac36b8a5710`.

No configured lint, type, or static-analysis tool was found in `pyproject.toml` beyond pytest.

## 5. Evidence Bundle

- Contract evidence: `RUNNER_CONTRACT` declares `schema_version=patchrail.runner_contract.v1`, `owns_canonical_state=False`, required workspace files `task.json`, `plan.json`, and `output.json`, reserved shell environment variables, runner-writable paths, and forbidden ownership of task lifecycle, canonical plan, review verdict, approval decision, and approval ledger.
- Harness evidence: `patchrail.runners.local_harness` persists `runner_trace.runner_contract` and the concrete workspace exchange paths used by the run.
- Shell runner evidence: `ShellConfiguredRunner` exports the existing required variables plus additive `PATCHRAIL_WORKSPACE`, `PATCHRAIL_ARTIFACT_DIR`, and `PATCHRAIL_TRACE_FILE`.
- Smoke evidence: local smoke completed with `Brief schema validation: schema=patchrail.brief_schema.v1 briefs=3`, final task state `approved`, and test coverage verifies the persisted `trace.json` exposes `patchrail.runner_contract.v1`.
- No secrets, credentials, migrations, deployments, browser MCP registration, browser debugging, or direct subordinate repository integrations were used.

## 6. Next highest-priority task

Continue Runner Contract v1 by adding compatibility/readiness checks for shell runner contract support and richer runner-local artifact ingestion, while keeping Patchrail as the canonical owner of evidence bundles, reviews, approvals, and ledgers.

## 7. Approval required: yes/no

No.
